version: 2

models:
  - name: fct_daily_revenue
    description: Daily completed-order revenue for the CEO dashboard.
    columns:
      - name: order_date
        data_tests:
          - not_null
          # The mart grain is one row per calendar day.
          - unique
      - name: completed_order_rows
        data_tests:
          - not_null
      - name: daily_revenue
        data_tests:
          - not_null
